<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css">
        <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" rel="stylesheet">

        <style>
            body {
                background-color: whitesmoke;
            }
            .navbar-custom {
                background-color: #1a2530;
            }
            .navbar-custom .navbar-brand,
            .navbar-custom .nav-link {
                color: #ffffff;
                font-weight: 500;
                text-transform: uppercase;
                letter-spacing: 0.5px;
                transition: color 0.3s ease;
            }
            .navbar-custom .nav-link:hover,
            .navbar-custom .navbar-brand:hover {
                color: #ff6b6b;
            }
            .dropdown-menu {
                background-color: #1a2530;
            }
            .dropdown-item {
                color: #ffffff;
            }
            .dropdown-item:hover {
                background-color: #ff6b6b;
                color: #ffffff;
            }
        </style>
        {% block stylesheets %}{% endblock %}
    </head>
    <body>
        {% if app.user %}
            <nav class="navbar navbar-expand-lg navbar-custom">
                <div class="container">
                    <a class="navbar-brand" href="{{ path('app_comenzi_index') }}">Sigstar Truck</a>
                    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarContent"
                            aria-controls="navbarContent" aria-expanded="false" aria-label="Toggle navigation">
                        <span class="navbar-toggler-icon" style="filter: brightness(0) invert(1);"></span>
                    </button>
                    <div class="collapse navbar-collapse" id="navbarContent">
                        <ul class="navbar-nav ms-auto">
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_parc_auto_index') }}">
                                    Parc Auto <i class="fas fa-car"></i>
                                </a>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="comenziDropdown" role="button"
                                   data-bs-toggle="dropdown" aria-expanded="false">
                                    Comenzi <i class="fas fa-truck"></i>
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="comenziDropdown">
                                    <li><a class="dropdown-item" href="{{ path('app_comenzi_index') }}">Comenzi</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_comenzi_comunitare_index') }}">Comenzi Comunitare</a></li>
                                </ul>
                            </li>
                            <li class="nav-item dropdown">
                                <a class="nav-link dropdown-toggle" href="#" id="cheltuieliDropdown" role="button"
                                   data-bs-toggle="dropdown" aria-expanded="false">
                                    Cheltuieli <i class="fas fa-money-bill-wave"></i>
                                </a>
                                <ul class="dropdown-menu" aria-labelledby="cheltuieliDropdown">
                                    <li><a class="dropdown-item" href="{{ path('app_categorii_cheltuieli_index') }}">Categorii Cheltuieli</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_consumabile_index') }}">Consumabile</a></li>
                                    <!-- NOU: link la rapoarte cheltuieli -->
                                    <li><a class="dropdown-item" href="{{ path('app_cheltuieli_list') }}">Rapoarte cheltuieli</a></li>
                                    <li><a class="dropdown-item" href="{{ path('app_cheltuieli_list') }}">Statistici cheltuieli</a></li>
                                </ul>
                            </li>
                            <li class="nav-item">
                                <a class="nav-link" href="{{ path('app_logout') }}">
                                    Logout <i class="fas fa-sign-out-alt"></i>
                                </a>
                            </li>
                        </ul>
                    </div>
                </div>
            </nav>
        {% endif %}

        <div class="container py-5">
            {% block body %}{% endblock %}
        </div>

        <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/js/bootstrap.bundle.min.js"></script>
        {% block javascripts %}{% endblock %}
    </body>
</html>
