{% extends 'base.html.twig' %}

{% block title %}Editează Mașină{% endblock %}

{% block body %}
    <h1>Editează Mașină</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Salvează</button>
    {{ form_end(form) }}
{% endblock %}