{% extends 'base.html.twig' %}

{% block title %}Adaugă Mașină{% endblock %}

{% block body %}
    <h1>Adaugă Mașină</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Salvează</button>
    {{ form_end(form) }}
{% endblock %}