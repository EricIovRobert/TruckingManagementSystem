{% extends 'base.html.twig' %}

{% block title %}Adaugă Categorie Cheltuieli{% endblock %}

{% block body %}
    <h1>Adaugă Categorie Cheltuieli</h1>
    {{ form_start(form) }}
        {{ form_widget(form) }}
        <button type="submit" class="btn btn-primary">Salvează</button>
    {{ form_end(form) }}
    <a href="{{ path('app_categorii_cheltuieli_index') }}" class="btn btn-secondary mt-3">Înapoi</a>
{% endblock %}